apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: mongo-express-policy
spec:
  selector:
    matchLabels:
      app: mongo-express
  action: ALLOW
  rules:
    - from:
        - source:
            requestPrincipals: ["*"]
